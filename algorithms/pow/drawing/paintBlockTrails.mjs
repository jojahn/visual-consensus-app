export const paintingOptions = {

}

export function paintBlockTrails(context, state, mouseCursor) {
  if (!state) {
    return [context.canvas.width, context.canvas.height];
  }
  return [context.canvas.width, context.canvas.height];
}
